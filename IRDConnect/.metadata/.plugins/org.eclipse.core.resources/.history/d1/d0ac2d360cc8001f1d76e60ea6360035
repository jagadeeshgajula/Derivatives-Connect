#include "Tenor.h"
#include <iostream>
#include "date.h"


using namespace std;

/*

Date operator + ( Date &date, Tenor & tenor )
{

        return date + tenor;
}

Date operator - ( Date & date, Tenor & tenor )
{
    return ::operator+( date, -tenor );
}

Date operator += ( Date & date, Tenor & tenor )
{
        return date = date + tenor;
}

Date operator -= ( Date & date, Tenor & tenor )
{
        return date = date - tenor;
}
*/
//----------------------------------------------------------------------


// End-Of-File ---------------------------------------------------------
